// Preload script to expose safe APIs if needed later.
// Keeping empty for now with contextIsolation enabled.
// We can expose versions or a limited bridge in the future.

// Example:
// const { contextBridge } = require('electron');
// contextBridge.exposeInMainWorld('env', { VERSION: '1.0.0' });


